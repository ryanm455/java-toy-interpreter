public enum TokenType {
    DECR,
    INCR,
    CLEAR,
    END,
    WHILE,
    EQ,
    NOT_EQ,
    DO,
    IDENTIFIER,
    INT,
    STR,
    SEMI,
    PLUS,
    MINUS,
    MULT,
    DIV,
    MOD,
    LPAREN,
    RPAREN,
    ASSIGN,
    DEF,
    RETURN,
    IF,
    ELSE,
    ELSE_IF,
    PRINT
}
